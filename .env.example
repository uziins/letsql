# Environment variables for testing
# Copy this file to .env and update with your database credentials

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_DATABASE=letsql_test

# Note: The test database will be used to create temporary test tables
# Make sure the database exists and the user has CREATE/DROP privileges
